---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: auths.containerregistry.arthurvardevanyan.com
spec:
  group: containerregistry.arthurvardevanyan.com
  names:
    kind: Auth
    listKind: AuthList
    plural: auths
    singular: auth
  scope: Namespaced
  versions:
    - name: v1beta1
      schema:
        openAPIV3Schema:
          description: Auth is the Schema for the auths API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: AuthSpec defines the desired state of Auth
              properties:
                containerRegistry:
                  default: quay
                  enum:
                    - quay
                  type: string
                quay:
                  description: Validations?
                  properties:
                    robotAccount:
                      description:
                        The Kubernetes Service Account That is Bound to for
                        Identity Federation
                      type: string
                  required:
                    - robotAccount
                  type: object
                secretName:
                  description: Name of the Secret to Save the Image Pull Secret Too
                  type: string
                serviceAccount:
                  description:
                    The Kubernetes Service Account That is Bound to for Identity
                    Federation
                  type: string
              required:
                - containerRegistry
                - quay
                - secretName
                - serviceAccount
              type: object
            status:
              description: AuthStatus defines the observed state of Auth
              properties:
                error:
                  description: Output of Any Errors
                  type: string
                tokenExpiration:
                  description: When the Current Token Expires
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
